test un command ----- dotnet run --project .\AudioBit.App\AudioBit.App.csproj

.\scripts\Build-ReleaseBundle.ps1


-redesign the entire UI using GPU-accelerated meters

-add device switching

-add per-app mute hotkeys

-add auto-start service

-Per-app output routing is one of the most powerful additions. Let users choose where each application sends audio. For example, Discord to headphones while Spotify goes to speakers. Windows already exposes this through the audio session APIs, but your UI would let users manage it quickly. Pair that with a quick default device switcher in the top bar so the user can flip between speakers, headphones, or Bluetooth devices without opening Windows settings.


-A useful companion is device hotkeys. A single keyboard shortcut could instantly switch the entire system output device.



-Add drag-to-reorder rows. Users often want Discord and Spotify pinned to the top.

- Add pinning:
Pinned apps stay visible even if silent.

- Add quick mute gestures:
Middle-click on an app row mutes it instantly.

- Add search filtering when many apps appear.


- Audio focus mode.
When a chosen app produces audio, automatically mute others.
-A remote control system where a native Android/iOS app connects to a specific PC running AudioBit by scanning a QR code or entering a one-time code. After pairing, the phone acts like a wireless control panel that works from anywhere through a cloud relay. The app shows a customizable soundboard and quick controls like mute microphone, change volumes, or switch audio devices. Pressing buttons on the phone sends commands to the PC in real time, similar to using a software Stream Deck for audio control.





 

NEW COMMANDS ADDED FOR VERSIONING AND RELEASES

- CI build/test commands:
dotnet restore AudioBit.sln
dotnet build AudioBit.sln -c Debug --no-restore
dotnet test AudioBit.Installer.Tests\AudioBit.Installer.Tests.csproj -c Debug

- Release build/test commands:
dotnet restore AudioBit.sln
dotnet build AudioBit.sln -c Release --no-restore
dotnet test AudioBit.Installer.Tests\AudioBit.Installer.Tests.csproj -c Release

- Install Velopack CLI:
dotnet tool install --global vpk --version 0.0.1298

- Publish the app for a version:
dotnet publish AudioBit.App\AudioBit.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:Version=X.Y.Z -p:AssemblyVersion=X.Y.Z -p:FileVersion=X.Y.Z -p:InformationalVersion=X.Y.Z -o publish\win-x64

- Download previous Velopack release metadata:
vpk download github --repoUrl https://github.com/ami-nope/AudioBit

- Pack a Velopack release:
vpk pack --packId AudioBit --packTitle AudioBit --packVersion X.Y.Z --channel stable --packDir publish\win-x64 --mainExe AudioBit.App.exe --icon AudioBit.App\Assets\AudioBit.ico --outputDir Releases

- Upload and publish the GitHub release:
vpk upload github --repoUrl https://github.com/ami-nope/AudioBit --publish --releaseName "AudioBit X.Y.Z" --tag X.Y.Z

- Git version tag commands:
git add .
git commit -m "Release X.Y.Z"
git tag X.Y.Z
git push origin main
git push origin X.Y.Z


STEP BY STEP: HOW TO MAKE THE NEXT VERSION AND RELEASE

1. Pick the next version number in semver format.
Example: if the current release is 1.4.0, the next one might be 1.4.1 or 1.5.0.

2. Finish the code changes you want in that release and verify the app still builds.
Use:
dotnet restore AudioBit.sln
dotnet build AudioBit.sln -c Debug --no-restore
dotnet test AudioBit.Installer.Tests\AudioBit.Installer.Tests.csproj -c Debug

3. Commit the release-ready changes.
Use:
git add .
git commit -m "Release X.Y.Z"

4. Create the Git tag for that exact version.
Use:
git tag X.Y.Z

5. Push the main branch and then push the version tag.
Use:
git push origin main
git push origin X.Y.Z

6. The GitHub Actions workflow .github/workflows/release-velopack.yml will run automatically from the tag push.
It does all of this:
- reads the version from the tag
- builds the app in Release
- runs tests
- publishes the app for win-x64
- packs the Velopack release
- uploads the release to https://github.com/ami-nope/AudioBit/releases

7. After the workflow finishes, open the GitHub Releases page and confirm the new release exists.
The tag should be in one of these formats:
X.Y
X.Y.Z
Legacy tags also accepted:
vX.Y
vX.Y.Z
Example:
1.5

8. Users must install the Velopack setup build from GitHub Releases to get automatic updates.
Legacy installs from the old custom installer do not auto-update.

9. AudioBit checks GitHub Releases on app launch only.
If a newer stable version exists, it downloads silently and asks for restart after download finishes.


LOCAL MANUAL RELEASE COMMAND FLOW

If you want to package locally before or instead of GitHub Actions, use:

dotnet tool install --global vpk --version 0.0.1298
dotnet restore AudioBit.sln
dotnet build AudioBit.sln -c Release --no-restore
dotnet test AudioBit.Installer.Tests\AudioBit.Installer.Tests.csproj -c Release
dotnet publish AudioBit.App\AudioBit.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:Version=X.Y.Z -p:AssemblyVersion=X.Y.Z -p:FileVersion=X.Y.Z -p:InformationalVersion=X.Y.Z -o publish\win-x64
vpk download github --repoUrl https://github.com/ami-nope/AudioBit
vpk pack --packId AudioBit --packTitle AudioBit --packVersion X.Y.Z --channel stable --packDir publish\win-x64 --mainExe AudioBit.App.exe --icon AudioBit.App\Assets\AudioBit.ico --outputDir Releases
vpk upload github --repoUrl https://github.com/ami-nope/AudioBit --publish --releaseName "AudioBit X.Y.Z" --tag X.Y.Z


IMPORTANT NOTES

- The shared app version now comes from Directory.Build.props.
- Local/dev default version is 0.0.0-local unless overridden by release publishing.
- Release tags now support X.Y or X.Y.Z directly.
- Legacy vX.Y and vX.Y.Z tags are still accepted.
- The supported updater release path is now Velopack.
- scripts\Publish-Release.ps1 now builds the custom AudioBit.Setup bootstrap installer with a Velopack portable payload so installs stay updater-friendly.


ONE COMMAND VELOPACK RELEASE

Preferred command:
.\scripts\Release-Velopack.ps1

What it does automatically:
- syncs with origin/current-branch first so the release commit is based on the latest remote branch
- reads version.json
- takes the currentVersion value
- increases the last number by 1
- writes the new version back into version.json
- runs restore, release build, and tests
- stages all current repo changes
- creates a commit named:
Release X.Y or Release X.Y.Z
- creates the matching git tag
- pushes the branch and the tag
- triggers the GitHub Actions Velopack release workflow
- waits until the GitHub release exists publicly
- verifies the published release includes setup installer, release notes, and Velopack feed assets

Examples:
- 1.6 becomes 1.7
- 1.9 becomes 1.10
- 1.6.4 becomes 1.6.5

Important:
- version.json is now the release source of truth for the one-command flow.
- the workflow checks that the pushed tag and version.json match exactly.
- the workflow generates release-notes markdown and passes it into vpk pack so the app updater can read update notes.
- the workflow explicitly verifies the setup installer asset and the RELEASES feed asset after publishing.
- the updater detects installed Velopack builds after users install from the GitHub release setup package.
- local dev builds are still not treated as updater-managed installs.


LOCAL RELEASE BUNDLE ZIP

Command:
.\scripts\Build-ReleaseBundle.ps1

What it does:
- reads the current version from version.json unless -Version is passed
- creates a folder named after that version under:
  artifacts\release-bundles\X.Y
- creates an updater-friendly setup folder at:
  artifacts\release-bundles\X.Y\AudioBit-Setup
- creates a zip named:
  artifacts\release-bundles\X.Y.zip
- puts only the updater-friendly Velopack release files in that setup folder
- includes the modern setup installer, RELEASES feed, full package, portable package, json manifests, and release notes in one place
- does not include the legacy custom installer flow

Useful example:
.\scripts\Build-ReleaseBundle.ps1 -Version 1.8


CUSTOM BOOTSTRAP INSTALLER FOLDER

Command:
.\scripts\Publish-Release.ps1

What it does:
- reads the current version from version.json unless -Version is passed
- builds the Velopack portable payload for that version in a temporary staging folder
- publishes the custom AudioBit.Setup installer into:
  artifacts\publish-ready\AudioBit-Setup
- copies the updater-friendly Velopack portable zip into:
  artifacts\publish-ready\AudioBit-Setup\payload\AudioBit-win-x64.zip
- keeps the familiar custom installer folder layout
- keeps auto-update support because the installed payload includes Update.exe, sq.version, and the Velopack launcher

Useful example:
.\scripts\Publish-Release.ps1 -Version 1.8


TWO MAIN LOCAL RELEASE COMMANDS

1. Only the custom bootstrap installer folder:
.\scripts\Build-BootstrapInstaller.ps1

Output:
artifacts\bootstrap-installer\X.Y\AudioBit-Setup

What it is for:
- share this folder directly with another laptop or PC
- install through the custom AudioBit.Setup UI
- still updater-friendly because the payload is the Velopack portable layout


2. New GitHub release folder with version bump:
.\scripts\Build-GitHubReleaseFolder.ps1

What it does:
- reads version.json
- bumps the version by 0.1
- writes the new version back into version.json
- creates:
  artifacts\github-release\NEW_VERSION\GitHub-Upload
  artifacts\github-release\NEW_VERSION\GitHub-Upload\AudioBit-Setup
  artifacts\github-release\NEW_VERSION\GitHub-Upload\AudioBit-Setup.zip
  artifacts\github-release\NEW_VERSION\GitHub-Upload\release-notes-brief.txt
- keeps the custom installer folder inside the GitHub upload folder
- also zips that custom installer folder automatically through the command
- also creates a brief text release note with at least 12 lines
- keeps both outputs updater-friendly
