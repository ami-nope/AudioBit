<UserControl x:Class="AudioBit.UI.AudioMeterControl"
             x:Name="RootControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DesignHeight="168"
             d:DesignWidth="460"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SliderBarButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Height="5"
                                CornerRadius="70"
                                Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Width="16"
                                Height="16"
                                CornerRadius="70"
                                Background="{DynamicResource TextPrimaryBrush}"
                                BorderThickness="0" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type Slider}">
            <Setter Property="Height" Value="30" />
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid Margin="0,7,0,0">
                            <Track x:Name="PART_Track"
                                   Minimum="{TemplateBinding Minimum}"
                                   Maximum="{TemplateBinding Maximum}"
                                   Value="{TemplateBinding Value}"
                                   IsDirectionReversed="{TemplateBinding IsDirectionReversed}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderBarButtonStyle}"
                                                  Background="{TemplateBinding Foreground}"
                                                  Command="Slider.DecreaseLarge" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderBarButtonStyle}"
                                                  Background="{DynamicResource SliderInactiveBrush}"
                                                  Command="Slider.IncreaseLarge" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MuteToggleButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="RootBorder"
                                Width="40"
                                Height="40"
                                Background="#242424"
                                BorderThickness="0"
                                CornerRadius="70">
                            <Grid>
                                <Viewbox Width="18" Height="18">
                                    <Grid>
                                        <Canvas x:Name="SpeakerIcon" Width="24" Height="24">
                                            <Path Fill="{DynamicResource TextPrimaryBrush}"
                                                  Data="M3,9 L7.5,9 L12,5.5 L12,18.5 L7.5,15 L3,15 Z" />
                                            <Path Stroke="{DynamicResource TextPrimaryBrush}"
                                                  StrokeThickness="1.9"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  Data="M15,9 C17,10.5 17,13.5 15,15" />
                                            <Path Stroke="{DynamicResource TextPrimaryBrush}"
                                                  StrokeThickness="1.9"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  Data="M17.5,6.5 C21,9.2 21,14.8 17.5,17.5" />
                                        </Canvas>
                                        <Canvas x:Name="MuteIcon" Width="24" Height="24" Opacity="0">
                                            <Path Fill="{DynamicResource AccentBrush}"
                                                  Data="M3,9 L7.5,9 L12,5.5 L12,18.5 L7.5,15 L3,15 Z" />
                                            <Path Stroke="{DynamicResource AccentBrush}"
                                                  StrokeThickness="2.2"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  Data="M15.5,8.5 L20.5,15.5 M20.5,8.5 L15.5,15.5" />
                                        </Canvas>
                                    </Grid>
                                </Viewbox>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource PanelHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="RootBorder" Property="Background" Value="#1F2A20" />
                                <Setter TargetName="SpeakerIcon" Property="Opacity" Value="0" />
                                <Setter TargetName="MuteIcon" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid MouseEnter="RootGrid_OnMouseEnter"
          MouseLeave="RootGrid_OnMouseLeave"
          Loaded="RootGrid_OnLoaded">
        <Border x:Name="CardBorder"
                Margin="0,0,0,18"
                Padding="20"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderThickness="0"
                CornerRadius="70"
                Opacity="0">
            <Border.RenderTransform>
                <TranslateTransform x:Name="CardTranslateTransform" Y="10" />
            </Border.RenderTransform>

            <Grid>
                <Border x:Name="HoverOverlay"
                        Background="{DynamicResource HoverOverlayBrush}"
                        CornerRadius="70"
                        Opacity="0" />

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0"
                                Grid.Column="0"
                                Orientation="Horizontal">
                        <Border Width="48"
                                Height="48"
                                Margin="0,0,16,0"
                                Background="#252525"
                                BorderThickness="0"
                                CornerRadius="70">
                            <Image Width="24"
                                   Height="24"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Source="{Binding Icon}"
                                   Stretch="Uniform" />
                        </Border>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock FontSize="16"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="{Binding AppName}" />
                            <TextBlock Margin="0,6,0,0"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Text="{Binding VolumePercentText}" />
                        </StackPanel>
                    </StackPanel>

                    <ToggleButton Grid.Row="0"
                                  Grid.Column="1"
                                  Style="{StaticResource MuteToggleButtonStyle}"
                                  IsChecked="{Binding IsMuted, Mode=TwoWay}" />

                    <Border Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Height="14"
                            Margin="0,18,0,20"
                            Background="{DynamicResource MeterTrackBrush}"
                            CornerRadius="70"
                            ClipToBounds="True">
                        <Border x:Name="MeterFill"
                                Background="{DynamicResource MeterFillBrush}"
                                CornerRadius="70"
                                RenderTransformOrigin="0,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="MeterScaleTransform" ScaleX="0" />
                            </Border.RenderTransform>
                        </Border>
                    </Border>

                    <Grid Grid.Row="2"
                          Grid.ColumnSpan="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Slider Minimum="0"
                                Maximum="1"
                                IsMoveToPointEnabled="True"
                                Value="{Binding Volume, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Column="1"
                                   Margin="16,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource TextTertiaryBrush}"
                                   Text="{Binding ProcessId, StringFormat=PID {0}}" />
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
