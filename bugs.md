# **AUDIOBIT BUGS AND UPDATES TO EXISTING FEATURES**

# |Installer|

- installer complete revamp its bullshit and need good design and things 
- 

## |Main AudioBit|

- the app looks too empty also the widgets look too empty and boring when nothin is playing 
- tbe master volume and output device bar is boring and permanent 
- the volume slider of the master volume is bs it is shit

## |settings |

- the spotify and discord and all future things should be added in settings panel called plugins make ithoselook premium and should be preloaded but shown to user as disabled and the user should be shown an install button and show a download animation and it turns on make m turned off by default 
- wire the auto reconnect remote in the settings 
- in the (close Button Behaviour) the minimise to Tray and Exit App is too small and like crushed and small height make it proper squrcle 
- the spotify and discord are too empty and big make them small and compact 
- remove the volume step size and the auto mute mic on soundboard

## |settings-advanced|

- give a button of checking update 
- remove the install type velopack thing 
- and make the test "Current Version" instead of "Version" 
- in Update status - only show "Latest"   
- change the text in Show Title Pin Button toggle to only 'PIN TO TOP' and no comments 
- only spotify widget toggle no comments 
- only spotify widget toggle no comments 
- the reset all settings doesnt work ig make sure its wired up fix its ui and the height of button and interaction 
- remove the export import profiles until a profile system is added 
- remove the Debug Mode thing 
- make the Google Sheets button small and a squrcle button named LOG 
- the LOG button should log previous 5 minutes and next 5 minutes instead of now being previous 5 mins only 
- the open log folder should be removed 
- the advanced setting opening thing has a squrcly hover animation remove that  and also in logs and the log folder 
- 

## |Spotify Widget|

- when a song is paused the visualiser has no baseline there should be a 0.5 px green bar 
- in the hover/click preview the song art is not rounded corners 
- add a volume visualiser around the pause/play circle like show the volume level 
- the web interface should contain spotify widget controls like it should be getting the controls from the app only there should be no separate auth in that remote thing 
- when ipass my cursor too fast over spotify widget the expanded view just remains and doesnt close ykw add a timer of 2 
- 

## ||Discord Widget|

- the circle outside the discord logo is misplaced its not exactly at centre 
- make the mic and deafen buttons a bit smaller 
- it should be added to the web interface to the right

## ||REMOTEBIT||

### |after connection|

- remove the one device per session text and via qr code scan  
- and in the connected through primary (ms) show only the ping like eg ms 
- in the remote device name show generic names like Windows PC for windows pc and Android for android or the device name and in brackets the type of generic names 
- fix the relay server its getting some failures recently
- remove the protocol tile
- add a show button in between connected and server and make it link to the qr code and sid page like clicking it shows a overlay of the qr code and sid btu when clicked outside of that shows back the connected 
- show no of remote devices connected if >1 give user a warning small msg that reconsider the devices review 
- then show the disconnect devices of multiple devices

### |before connection|

- the text below qr code should be "Scan with AudioBit Mobile/web to connect " 
and provide download links to them 
- the show connected device is not completely showing make it only show "connected devices"
- in the sid and code remove the session policy  
  
  
